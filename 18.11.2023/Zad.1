#include <iostream>
#include <cmath>
using namespace std;

bool szescian(int a)
{
    for (int i = 0; i <= a / 4; i++)
        if (i * i * i == a)
            return 1;
    return 0;
}

int main()
{
    int n;
    cin >> n;
    cout << " " << szescian(n) << endl;
}
